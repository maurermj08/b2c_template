// Shared Prisma client — re-export for use across all apps
// For now, each app maintains its own Prisma schema and client.
// This package is a placeholder for when you want to centralize
// the database layer.
export {};
