# Root-level env — used by Prisma CLI commands run from the repo root.
# Each app also has its own .env with dev defaults.
# Override any value by creating .env.local (gitignored).

DATABASE_URL="file:./dev.db"
